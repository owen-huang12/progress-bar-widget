services:
  - type: web
    name: progress-bar-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: PORT
        value: 8080
    healthCheckPath: /
    autoDeploy: true
